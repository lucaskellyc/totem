# Local dev loads assets from Vite's own server (repo `assets/` folder), so new
# or untracked files work immediately without pushing them to the CDN branch.
# Production build has no override and falls back to the jsDelivr CDN.
VITE_ASSET_URL=/assets
